// import { getToken, setToken, removeToken, setTimeStamp } from '@/utils/auth'

const state = {}
const mutations = {}
const actions = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
